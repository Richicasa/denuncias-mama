[project]
name = "denuncias-app"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "pillow>=12.3.0",
    "playwright>=1.62.0",
    "python-multipart>=0.0.32",
    "python-telegram-bot>=22.8",
    "qrcode>=8.2",
    "requests>=2.34.2",
    "uvicorn[standard]>=0.52.4",
    "winocr>=0.0.15",
]

[project.scripts]
denuncias-app = "denuncias_app:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
